<%= simple_form_for(@tracking) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :value %>
    <%= f.input :units %>
    <%= f.association :catogory %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
